import './ScoreCounter.css';

const scoreCounter = () => {

    return (
        <div className="score-counter">
            i am the score counter
        </div>
    );
};

export default scoreCounter;