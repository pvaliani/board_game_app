import Button from '../Button/Button';
import './ResetBox.css';

const ResetBox = () => {

    return (
        <div className="reset-box">
            <Button title="Reset"/>
        </div>
    );
};

export default ResetBox;